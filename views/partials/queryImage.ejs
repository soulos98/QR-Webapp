<section class="QueryImage mainblock">
    <div>
        <% if(locals.qrCode) {%>
            <img id="qrValue" src="<%= qrCode %>" alt="">
        <%} else {%>
            <img src="pictures/doggo.jpg" alt="doggo">
        <% } %>
    </div>
</section>